import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    boxLogo: {
        marginBottom: 40,
        alignItems: 'center',
    },
    logo: {
       width: 260,
       height: 50,
    }
});

export default styles